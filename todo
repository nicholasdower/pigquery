Add light mode.
Unify styles in extension popup and modal.
Consider allowing multiple configs.
Create screenshots and/or a gif.
Publish to web store.
Organize repo files (images dir, etc.).
Add bug link to popup or settings.
Add tests.
Show windows symbol in popup.
Maybe support multiple tags.
Clean up global state in content script.
Allow override of keyboard shortcuts.
Link to named queries. Problem is not all users have the same config. Maybe include full query in URL as backup.
Link to config from no options found message.
Add buttons to site link to open in background tab, new window, etc.
It should be possible to copy a link to a site by right-clicking
It should be possible to disable groups.
Once a group is used, maybe this should add a bonus to that group in the search rankings.
Precreate search index.
Add a refresh config button to the popup. Show when last refreshed. Show errors.
Show config-specific errors from last fetch on each remote config.
Add link to chrome extension settings from popup.
